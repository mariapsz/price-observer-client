import styled from 'styled-components';

export const PasswordInput = styled.input`
  border: none;
  flex-basis: 85%; 
  
  :invalid:focus  {
    outline: none;
  }
  
  :focus  {
    outline: none;
  }
`;
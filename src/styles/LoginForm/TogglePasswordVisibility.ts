import styled from 'styled-components';

export const TogglePasswordVisibility = styled.i`
    font-size: 24px;
    align-self: center;
    :hover {
      cursor: pointer;
      color: #de9350;
      transition: 0.2s;
    }
`;